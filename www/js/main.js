"use strict";

function main()
{
    wn2nac.init();
    wn2nac.start();
    wn2nac.setCallback(onEvent);
}

function onEvent(data)
{
    /*switch(data.dataType)
    {
        case n:
            code block
            break;
        case n:
            code block
            break;
        default:
            default code block
    }*/
}
